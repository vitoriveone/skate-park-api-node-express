    <div class="py-5">
      <h2>{{title}}</h2>
      <hr class="w-50" />
      <form id="loginForm">
        <div class="form-group">
          <div class="form-group">
            <label>Email</label>
            <input class="form-control w-50 m-auto" id="loginEmail" type="email"/>
          </div>
          <div class="form-group">
            <label>Password</label>
            <input class="form-control w-50 m-auto" id="loginPass" type="password"/>
          </div>
        </div>
        <button class="btn btn-success mb-3">Ingresar</button>
        <p>¿Aún no tienes cuenta? <a href="/register">Regístrate</a></p>
      </form>
    </div>